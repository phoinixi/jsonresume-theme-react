// vitest.setup.ts

// Import Jest-DOM for its side effects, which automatically extends expect
import '@testing-library/jest-dom';

// We no longer need to import expect or matchers explicitly here
// import { expect } from 'vitest';
// import matchers from '@testing-library/jest-dom/matchers';
// expect.extend(matchers); // This is now handled by the import above
